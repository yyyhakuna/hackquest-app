'use client'

export function Test() {
  // const { data: blogsData, isPending } = useSuspenseQuery(blogsQueryOptions)
  // const { data } = useSuspenseQuery(blogQueryOptions('d25b40b7-9a67-4ebd-8baf-b3997dea8732'))

  // const { mutate } = useMutation({
  //   mutationFn: async () => execute(UpdateBlogDocument, { updateBlogUpdateBlogInput3: { id: 1 } }),
  // })

  return (
    <>
      {/* <h1 className="display-1">{data.blog.title}</h1> */}
      {/* <Button onClick={() => mutate()}>测试mutation</Button> */}
      {/* <div>
        {blogsData.blogs.list?.map(blog => {
          return <div key={blog.id}>{blog.title}</div>
        })}
      </div> */}
    </>
  )
}
